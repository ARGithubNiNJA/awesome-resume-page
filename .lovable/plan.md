Move `files/resume.html` to the project root as `resume.html`.

## What will change
- Source: `files/resume.html`
- Destination: `resume.html` (project root)
- The file contents remain unchanged.
- The `files/` folder will be empty after the move; it can be removed if desired.

## Steps
1. Move `files/resume.html` to `/dev-server/resume.html`.
2. Optionally remove the now-empty `files/` directory.

No other files or routes are affected.